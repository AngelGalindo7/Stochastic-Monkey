{
  "name": "heuristic-monkey",
  "version": "0.1.0",
  "description": "MCTS + LLM-driven stochastic web bug hunter. Local-only v1.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "audit:config": "node src/index.js --audit-config"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/resources": "^1.30.1",
    "@opentelemetry/sdk-trace-base": "^1.30.1",
    "dotenv": "^17.4.2",
    "openai": "^4.77.0",
    "puppeteer": "^24.40.0",
    "seedrandom": "^3.0.5",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
